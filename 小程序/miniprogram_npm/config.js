/**
 * AlphaMind 超级配置文件
 * 作用：低代码控制中心，无需修改业务代码即可调整 App 行为
 */
const config = {
  // =========================================================================
  // 1. 核心连接配置 (Connectivity)
  // =========================================================================
  // 🔴 关键修复：从 'your-api-domain' 改回真实的腾讯云 IP
  API_BASE_URL: "http://111.230.52.99:8000", 
  
  // 🔴 关键修复：填入你提供的云环境 ID
  CLOUD_ENV_ID: "cloud1-8gcsrusq05113bb6",

  // =========================================================================
  // 2. AI 策略阀门 (Strategy Thresholds)
  // =========================================================================
  // 决定 LSTM 模型的“胆量”。置信度 > HIGH 判涨，< LOW 判跌
  THRESHOLD_HIGH: 0.6, 
  THRESHOLD_LOW: 0.4,

  // 默认风险画像（用于游客或未登录状态的兜底逻辑）
  DEFAULT_RISK: "稳健型",

  // =========================================================================
  // 3. 视觉与国际化 (Visual & I18n)
  // =========================================================================
  // 颜色模式：true = A股模式 (红涨绿跌)
  IS_CN_MARKET: true, 

  // 全局色板 (Dark Fintech 风格)
  COLORS: {
    UP: "#E74C3C",   // 涨：赛博红 (比默认的红更高级)
    DOWN: "#00C853", // 跌：荧光绿 (比默认的绿更亮)
    FLAT: "#9E9E9E", // 平：灰色
    PRIMARY: "#0052D4", // 主题蓝
    BG: "#0A1E36"    // 背景深蓝
  },

  // =========================================================================
  // 4. 界面文案 (Copywriting)
  // =========================================================================
  // 之前的代码依赖这些字段，如果删了会报错，必须补回来
  TEXT: {
    LOADING: "AI 算力接入中...",
    SEARCH_PLACEHOLDER: "输入代码 (如 600519 / AAPL)",
    AI_TITLE: "AlphaMind 投资锦囊",
    EMPTY_HISTORY: "暂无足迹，去探索市场吧"
  }
};

module.exports = config;